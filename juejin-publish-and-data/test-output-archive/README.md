## Test Output Archive

This directory stores historical test artifacts that no longer match the current JSON-only output contract.

Archived here:
- legacy-format-20260311: pre-2026-03-12 outputs, including the old Markdown report, old JSON shape with records/metrics, and temporary probe data.

Current validated output remains in:
- ../test-output-live-v2/